{
  "name": "PikaPerpV2",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ PikaPerpV2",
    "create-local": "graph create --node http://localhost:8020/ PikaPerpV2",
    "remove-local": "graph remove --node http://localhost:8020/ PikaPerpV2",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 PikaPerpV2"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.21.0",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
